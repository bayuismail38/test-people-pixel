###
HOW TO RUN IT

first make sure u got your postgres turn on. n run npm run install && npm run dev && npm run migrate

you can import this postman file i gave. n if u want to do online sessions. i ve already deploy it

deployed services: https://test-people-pixel.onrender.com

heres the env for local

DB_USER=postgres
DB_PASSWORD=yourpassword
DB_HOST=localhost
DB_PORT=5432
DB_NAME=yourdbname